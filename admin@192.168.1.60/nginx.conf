server {
    listen 80;
    server_name your-domain.com;      # replace with your VM's IP or domain

    # Proxy all requests to the FastAPI app
    location / {
        proxy_pass         http://127.0.0.1:8000;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_read_timeout 60s;
    }

    # Serve static files directly through nginx (bypasses Python)
    location /static/ {
        alias   /opt/bakery/static/;
        expires 30d;
        add_header Cache-Control "public, immutable";
    }

    # Optional: increase max upload size if you add image uploads later
    client_max_body_size 10M;
}
